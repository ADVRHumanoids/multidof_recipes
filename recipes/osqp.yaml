clone:
  type: git
  server: github.com
  repository: oxfordcontrol/osqp.git
  tag: master
  tag_if: 
    casadi_stable: release-0.6.3
  recursive: true

build:
  type: cmake
  args:
    - -DDLONG=OFF 
    - -DPRINTING=OFF
    - -DPROFILING=OFF
    - -DENABLE_MKL_PARDISO=OFF

depends: []


