clone:
  type: git
  server: github.com
  repository: oxfordcontrol/osqp.git
  tag: 0dd00a5 # required by current version of casadi, as master is no longer compatible
  recursive: true

build:
  type: cmake
  args:
    - -DDLONG=OFF 
    - -DPRINTING=OFF
    - -DPROFILING=OFF
    - -DENABLE_MKL_PARDISO=OFF

depends: []


