clone:
  type: git
  server: github.com
  tag: caab299
  repository: advrhumanoids/xbot_msgs.git

pre_build_if:
  binary_gen:
    - rm -rf *.deb

build:
  type: cmake
  skip_if: ubuntu_release < 18.04

post_build_if:
  binary_gen:
    - make package
    - cp *.deb {config.binary_dir}

depends: []

