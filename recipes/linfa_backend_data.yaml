clone:
  type: git
  server: github.com
  repository: advrhumanoids/linfa_backend_data.git
  tag: v1.0.0-alpha.4.1
  tag_if:
    experimental: development

build:
  type: custom
  cmd:
    - cd {srcdir} && npm ci

depends:
  - web-video-server
  - rosbridge-server
  - linfa_ros
