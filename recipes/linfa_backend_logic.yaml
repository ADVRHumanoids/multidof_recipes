clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/linfa_backend_logic.git
  tag: v0.0.2
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - capability_framework
  - linfa_ros