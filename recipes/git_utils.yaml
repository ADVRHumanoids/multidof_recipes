clone:
  type: git
  server: github.com
  repository: marcoruzzon/git_utils.git
  tag: 4f1bca8
  
build:
  type: custom
  cmd:
    - mkdir {installdir}/bin | true
    - ln -s {srcdir}/scripts/* {installdir}/bin