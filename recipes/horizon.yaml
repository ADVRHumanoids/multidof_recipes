clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/horizon
  tag: ros2_merge
  
build:
  type: custom
  cmd:
    - pip install -e {srcdir} --no-deps
  
depends:
  - casadi_kin_dyn
  
system_depends:
	- swig
