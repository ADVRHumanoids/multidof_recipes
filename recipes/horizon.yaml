clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/horizon
  tag: receding_horizon
  
build:
  type: custom
  cmd:
    - pip install -e {srcdir} --no-deps
  
depends:
  - casadi_kin_dyn
