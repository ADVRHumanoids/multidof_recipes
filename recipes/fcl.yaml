clone:
  type: git
  server: github.com
  repository: flexible-collision-library/fcl.git
  tag: v0.6.1

build:
  type: cmake
  args:
    - -DBUILD_TESTING=FALSE 
    - -DFCL_STATIC_LIBRARY=TRUE 
    - -DFCL_COVERALLS_UPLOAD=FALSE

depends: []


