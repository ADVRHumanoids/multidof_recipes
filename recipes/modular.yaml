clone:
  type: git
  server: github.com
  repository: advrhumanoids/modular_hhcm.git
  tag: ros2-merge-no-tf
  recursive: true
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}
