clone:
  type: git
  server: github.com
  repository: advrhumanoids/modular.git
  tag: pino/linfization
  tag_if:
    development: feat/linfization
    experimental: feat/linfization
  recursive: true
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - xacro
