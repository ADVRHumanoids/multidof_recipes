clone:
  type: git
  server: github.com
  repository: advrhumanoids/modular_hhcm.git
  tag: pino_jl
  tag_if:
    development: pino_jl
    experimental: pino_jl
  recursive: true
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - xacro
