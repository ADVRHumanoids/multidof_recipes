clone:
  type: git
  server: github.com
  repository: advrhumanoids/modular_hhcm.git
  tag: master
  recursive: true
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - xacro
