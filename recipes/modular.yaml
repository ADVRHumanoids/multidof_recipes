clone:
  type: git
  server: github.com
  repository: advrhumanoids/modular.git
  tag: tapullo_pino
  recursive: true
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - xacro
