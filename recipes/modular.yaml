clone:
  type: git
  server: github.com
  repository: advrhumanoids/modular.git
  tag: master
  tag_if:
    development: master
    experimental: master
  recursive: true
  ros_src: true

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - xacro
