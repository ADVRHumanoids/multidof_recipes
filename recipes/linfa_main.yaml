clone:
  type: git
  server: github.com
  repository: advrhumanoids/linfa_main.git
  tag: v1.0.0-alpha.4.1
  
build:
  type: custom
  cmd:
    - cd {srcdir} && npm ci

depends:
  - web-video-server
  - rosbridge-server
