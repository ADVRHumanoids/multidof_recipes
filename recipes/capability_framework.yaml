clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/capability_framework.git
  tag: v0.0.4

build:
  type: custom
  cmd:
    - pip install -e {srcdir}

depends:
  - albero-api
  - moveit-commander   # for collision